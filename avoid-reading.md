---
type: Note
---
# avoid reading

### simple avoidance

1. 查询附近的行人和车辆
2. 判断行人之间的方向。距离过小时需要停止，并强制偏移，正前方需要减速，侧前方需要稍微偏移位置，计算出sideSign
3. 判断行人和车的方向，距离过小时强制偏移。把相对速度在相对位置上进行投影，算出还有多久碰撞，如果小于0 代表不会碰撞，算出时间以后再算出最小间距，如果小于判断范围就偏移，计算出sideSign
4. 判断是否死锁了（长时间卡住），如果是就强制位移
5. 通过目标方向和偏移方向插值出一个值，进行位移
